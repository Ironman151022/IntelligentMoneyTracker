Optional asset for the spike path:

- Add `spike_sample.wav` beside this file if you want the "Run audio spike" button
  to feed a short WAV clip into Gemma through LiteRT-LM.
- The current implementation already supports audio if that file exists.
- Until then, "Run text spike" is the quickest end-to-end validation path.
